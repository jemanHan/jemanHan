type Points = 20 | 30 | 40 | 50;
let score: Points = 40;

console.log(score)

type ComplexPerson = {
    name:string,
    age:number,
    birthdate:Date,
    married:boolean,
    address: string
}

type Type1 = number;
type Type2 = string;
// boolean, {}, intersection, union, .........